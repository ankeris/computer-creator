import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
    container: {
        width: "400px",
        height: "700px",
        margin: "30px auto 0",
        overflow: "auto",
        padding: 12,
        boxShadow: "0 0 40px -10px grey",
    },
}));
